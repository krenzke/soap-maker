<%= form_with(model: batch_line_item) do |form| %>
  <% if batch_line_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(batch_line_item.errors.count, "error") %> prohibited this batch_line_item from being saved:</h2>

      <ul>
        <% batch_line_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quantity_oz, style: "display: block" %>
    <%= form.text_field :quantity_oz %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
